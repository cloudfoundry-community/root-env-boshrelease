---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - root_env
  stemcell:
    name: STEMCELL
  root_env:
    env_tarball: TARBALL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: root-env
    version: latest
